services:
  adlauncher:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: adlauncher-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
