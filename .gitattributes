# Shell scripts must keep LF endings. Checked out with CRLF on Windows and then
# run on the Mac mini, a script fails with "bad interpreter: no such file or
# directory" — the \r becomes part of the shebang path.
.githooks/* text eol=lf
*.sh        text eol=lf
